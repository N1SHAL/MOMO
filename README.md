## momo 💘 (Valentine Website)

## Run locally
1. Open `index.html` in any browser (Chrome/Safari).
   - Tip: On iPhone, save these files to Files app, then open `index.html`.

## Deploy on GitHub Pages (free)
1. Create a GitHub repo (example: `momo`)
2. Upload these 3 files to the repo root:
   - `index.html`
   - `style.css`
   - `script.js`
3. Go to **Settings → Pages**
4. Under **Build and deployment**:
   - Source: Deploy from a branch
   - Branch: `main` / root
5. Save — your site will be live at:
   `https://YOUR_GITHUB_USERNAME.github.io/momo/`
